<% provide :title, t("admin_reviews.base_title") %>
<div class="">
  <div class="page-title">
    <div class="title_left">
      <h3><%= t "admin_reviews.base_title" %></h3>
    </div>
  </div>
  <div class="clearfix"></div>

  <div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
      <div class="x_panel">
        <div class="x_title">
          <h2><%= t "admin_reviews.all" %></h2>
          <ul class="nav navbar-right panel_toolbox">
            <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
            </li>
          </ul>
          <div class="clearfix"></div>
        </div>
        <div class="x_content">
          <table id="users-table" class="table table-striped table-bordered">
            <thead>
              <tr>
                <th><%= t "admin_reviews.id" %></th>
                <th><%= t "admin_reviews.name" %></th>
                <th><%= t "admin_reviews.place" %></th>
                <th><%= t "admin_reviews.author" %></th>
                <th><%= t "admin_manage.action" %></th>
            </thead>
            <tbody>
              <% @reviews.each do |review| %>
                <tr>
                  <td><%= review.id %></td>
                  <td><%= link_to review.title, admin_review_path(review) %></td>
                  <td><%= link_to review.place.name, admin_place_path(review.place) %></td>
                  <td><%= review.user.username %></td>
                  <td>
                    <%= link_to [:admin, review], method: :delete, class: "btn btn-round btn-danger btn-xs" do %>
                      <i class="fa fa-user-circle-o"></i>  <%= t "admin_manage.delete" %>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
<script type="text/javascript">
  $(document).ready(function () {
    //DataTable
    $('#users-table').DataTable();
  })
</script>
